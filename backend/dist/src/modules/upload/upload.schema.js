import { z } from 'zod';
// Currently, we don't have complex validation for file uploads
// but we keep this file for architectural consistency.
export const uploadSchema = z.object({
// Add schema definitions if needed (e.g., for metadata)
});
//# sourceMappingURL=upload.schema.js.map