git reset --soft HEAD~1

./scripts/run_phone.sh --wifi-connect 192.168.1.26
# or

./scripts/run_phone.sh --real-device #wifi

npx ts-node scripts/fast_forward.ts

npm run create-admin -- "Admin" "12345679" "12345679"

docker compose exec backend npm run prisma:mig
docker compose exec backend npm run build


docker compose exec backend npm run fast_forward

npx kill-port 3000

docker compose down
docker compose build --no-cache backend
docker compose up

docker compose up --build


#**************************backend**************************
npm run prisma:migrate   # changed schema → migrate + auto-generate
npm run prisma:generate  # after npm install or git pull
npm run prisma:reset     # nuke DB and start fresh (careful in production!)
npm run prisma:studio    # open visual DB browser
npm run dev              # start server

#**************************flutter**************************


docker compose --profile build up --build apk-builder
flutter build apk --release