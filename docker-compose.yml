version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: 7jemty-backend
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    restart: unless-stopped

  flutter:
    build:
      context: ./flutter_application_1
      dockerfile: Dockerfile
    container_name: 7jemty-flutter
    ports:
      - "8080:8080"
    volumes:
      - ./flutter_application_1:/app
      # Cache Flutter's internal directories to avoid overriding from host
      - /app/.dart_tool
      - /app/build
      - /app/linux
      - /app/windows
      - /app/macos
    restart: unless-stopped
